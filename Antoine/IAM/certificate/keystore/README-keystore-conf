#Import LDAPS certificate in java keystore/truststore
sudo keytool -importcert -cacerts -trustcacerts -storepass keystore-mdp -noprompt -alias cyberdiploma.corp -file server-ldaps.cer

#List certificates in java keystore for verification 
keytool -list -keystore keystore/keystore.jks

